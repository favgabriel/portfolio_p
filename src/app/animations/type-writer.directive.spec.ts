import { TypeWriterDirective } from './type-writer.directive';

describe('TypeWriterDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeWriterDirective();
    expect(directive).toBeTruthy();
  });
});
